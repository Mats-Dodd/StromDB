[graph]
all-features = true

[advisories]
yanked = "deny"
ignore = [
    # rsa (Marvin Attack, no fixed release) is an unconditional dependency of
    # secretspec's provider machinery. strom-server loads secrets once at
    # process start on the operator's host; no RSA operation is exposed to
    # network-observable timing. Remove when secretspec gates or drops rsa.
    "RUSTSEC-2023-0071",
]

[licenses]
version = 2
allow = [
    "MIT",
    "MPL-2.0",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "Unicode-3.0",
    # Permissive licenses carried by the rustls TLS stack beneath object_store:
    # ISC (rustls-webpki, untrusted, aws-lc), BSD-3-Clause (subtle, aws-lc-sys),
    # CDLA-Permissive-2.0 (webpki-root-certs data).
    "ISC",
    "BSD-3-Clause",
    "CDLA-Permissive-2.0",
]

[bans]
multiple-versions = "warn"
wildcards = "deny"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
