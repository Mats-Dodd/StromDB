# Plan of attack

The first thing to build will be the ledger.  This is a good first cut as it lets use tackle both the first store and the correctness mdoel at the same time, which are inherently interleaved.  The store itself is simple enough and is needed by all other operations downstream.  

