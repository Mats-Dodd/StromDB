allow-renamed-params-for = [
  "..",
  "core::fmt::Debug",
  "core::fmt::Display",
]

missing-docs-in-crate-items = false

# ".." keeps clippy's defaults (foo, baz, quux). The rest enforce stromstyle §1
# and §11: precise domain nouns, no generic names, no nonstandard abbreviations.
disallowed-names = [
  "..",
  "data",
  "item",
  "items",
  "manager",
  "mgr",
  "info",
  "helper",
  "util",
  "tmp",
  "temp",
  "obj",
  "thing",
  "res",
  "src",
  "dst",
  "dest",
]

disallowed-methods = [
  { path = "std::time::SystemTime::now", reason = "wall time enters through an explicit Clock seam" },
  { path = "std::time::SystemTime::elapsed", reason = "wall time enters through an explicit Clock seam" },
  { path = "std::time::Instant::now", reason = "monotonic time enters through an explicit Clock seam" },
  { path = "std::time::Instant::elapsed", reason = "monotonic time enters through an explicit Clock seam" },
  { path = "std::thread::sleep", reason = "sleeping enters through an explicit Clock seam" },
  { path = "rand::random", allow-invalid = true, reason = "randomness enters through an explicit Entropy seam" },
  { path = "rand::rng", allow-invalid = true, reason = "randomness enters through an explicit Entropy seam" },
  { path = "rand::rngs::thread::rng", allow-invalid = true, reason = "randomness enters through an explicit Entropy seam" },
  { path = "rand::random_range", allow-invalid = true, reason = "randomness enters through an explicit Entropy seam" },
  { path = "rand::random_bool", allow-invalid = true, reason = "randomness enters through an explicit Entropy seam" },
  { path = "rand::random_ratio", allow-invalid = true, reason = "randomness enters through an explicit Entropy seam" },
  { path = "rand::random_iter", allow-invalid = true, reason = "randomness enters through an explicit Entropy seam" },
  { path = "std::fs::read", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::write", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::read_to_string", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::read_dir", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::File::open", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::File::create", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::File::create_new", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::OpenOptions::new", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::remove_file", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::remove_dir", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::remove_dir_all", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::rename", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::copy", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::hard_link", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::metadata", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::symlink_metadata", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::canonicalize", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::create_dir", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::create_dir_all", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::set_permissions", reason = "durable state enters through an explicit storage seam" },
  { path = "std::fs::exists", reason = "durable state enters through an explicit storage seam" },
  { path = "std::path::Path::exists", reason = "durable state enters through an explicit storage seam" },
  { path = "std::path::Path::try_exists", reason = "durable state enters through an explicit storage seam" },
  { path = "std::path::Path::metadata", reason = "durable state enters through an explicit storage seam" },
  { path = "std::path::Path::canonicalize", reason = "durable state enters through an explicit storage seam" },
  { path = "std::path::Path::read_dir", reason = "durable state enters through an explicit storage seam" },
  { path = "std::env::var", reason = "configuration is parsed at the boundary and passed in as data" },
  { path = "std::env::var_os", reason = "configuration is parsed at the boundary and passed in as data" },
  { path = "std::env::vars", reason = "configuration is parsed at the boundary and passed in as data" },
  { path = "std::env::vars_os", reason = "configuration is parsed at the boundary and passed in as data" },
]

disallowed-types = [
  { path = "std::collections::HashMap", reason = "iteration order varies between runs; use BTreeMap" },
  { path = "std::collections::HashSet", reason = "iteration order varies between runs; use BTreeSet" },
  # Interior mutability is a deliberate decision (stromstyle §5). Each use gets an
  # #[expect] whose reason names the single writer, the invariant, and the shutdown path.
  { path = "std::sync::Mutex", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::RwLock", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::OnceLock", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::LazyLock", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::cell::RefCell", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::cell::Cell", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::cell::OnceCell", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::cell::LazyCell", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicBool", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicI8", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicI16", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicI32", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicI64", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicIsize", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicU8", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicU16", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicU32", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicU64", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicUsize", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
  { path = "std::sync::atomic::AtomicPtr", reason = "interior mutability must be enumerable; name writer and invariant via #[expect]" },
]

disallowed-macros = [
  { path = "core::debug_assert", reason = "assertions run in every build; demote only with a measured cost reason via #[expect]" },
  { path = "core::debug_assert_eq", reason = "assertions run in every build; demote only with a measured cost reason via #[expect]" },
  { path = "core::debug_assert_ne", reason = "assertions run in every build; demote only with a measured cost reason via #[expect]" },
]
