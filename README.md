# StromDB

StromDB is a database project written in Rust.

## Development

The repository is a Cargo workspace. Install `just`, `cargo-nextest`,
`cargo-deny`, `cargo-machete`, and `cargo-hack`, then run:

```sh
just ci
```

Use `just --list` to see the individual checks and development commands.
