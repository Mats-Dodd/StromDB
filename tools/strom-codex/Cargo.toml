[package]
name = "strom-codex"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Extracts and checks the Strom Codex: RFC decision records and their statement index."
publish = false

# Deliberately not a workspace member: this is repository tooling, not engine
# code, and it is exempt from the workspace lint regime (see AGENTS.md).
# The empty [workspace] table stops cargo from attaching this package to the
# root workspace.
[workspace]

[dependencies]
